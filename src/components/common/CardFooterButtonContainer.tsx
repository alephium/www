import styled from 'styled-components'

const CardFooterButtonContainer = styled.div`
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  margin-top: var(--spacing-4);
`

export default CardFooterButtonContainer
