import styled from 'styled-components'

import PageSectionContainer from '../PageSectionContainer'

const HeroPageSectionContainer = styled(PageSectionContainer)`
  display: flex;
  flex-direction: column;
  flex: 1;
  height: auto;
`

export default HeroPageSectionContainer
